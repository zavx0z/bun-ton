{
  "name": "nft",
  "dependencies": {
    "@libs/ton": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "deploy": "bun --env-file=../../.env blueprint run",
    "test": "bun --env-file=../../.env test --timeout 4444"
  }
}
